{
  "name": "legacy-of-etrio-monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd shared && npm install && cd ../server && npm install && cd ../client && npm install",
    "build:shared": "cd shared && npm run build || true",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "build": "npm run build:shared && npm run build:server",
    "deploy:server": "cd server && npx wrangler deploy --name=etrio-api",
    "deploy:client": "cd client && npx wrangler pages deploy dist --project-name=etrio-web",
    "deploy": "cd server && npx wrangler deploy --name=etrio-api"
  }
}
